{
  "name": "codeninjas-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:db:up": "docker start dojolink-test-db 2>/dev/null || docker run -d --name dojolink-test-db -e POSTGRES_PASSWORD=testpass -e POSTGRES_DB=dojolink_test -p 55432:5432 postgres:17",
    "test:db:down": "docker rm -f dojolink-test-db"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "connect-pg-simple": "^9.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "express-rate-limit": "^8.5.2",
    "express-session": "^1.18.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^4.1.10"
  }
}

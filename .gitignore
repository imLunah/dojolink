node_modules/
client/node_modules/
server/node_modules/
client/dist/
server/data/
.env
.claude/
.agents/
CLAUDE.md

# Editor swap + local test artifacts
.CLAUDE.md.swp
*.swp
test-results/
.DS_Store
.vercel

# integration-test dump logs
test/db/*.log

# local tooling lockfile, not part of the app
skills-lock.json

# Local worksheet for naming the JR / Robotics / AI curriculum modules.
# Franchise curriculum, and this repo is public: it stays on disk only.
MODULE_NAMES.md
